import { TemplateResult } from 'lit-html';
import { LuminaJsx } from '@arcgis/lumina';
import { Scale } from '../interfaces';
export interface XButtonProps extends LuminaJsx.CustomAttributes {
    disabled: boolean;
    label: string;
    scale: Scale;
    onClick?: LuminaJsx.DOMAttributes<HTMLElement>["onClick"];
}
export declare const CSS: {
    button: string;
};
export declare const XButton: ({ disabled, key, label, scale }: XButtonProps) => TemplateResult;
