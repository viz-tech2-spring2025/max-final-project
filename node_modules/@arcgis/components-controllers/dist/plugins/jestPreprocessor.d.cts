import { SyncTransformer } from '@jest/transform';

/**
 * While we have a Rollup plugin that does the necessary modifications to power
 * Controllers, when running Stencil tests, Rollup is only used for end-to-end
 * tests. Unit tests don't run Rollup, and so this Jest transformer is provided
 * instead
 */

declare const transformer: SyncTransformer;

export { transformer as default };
